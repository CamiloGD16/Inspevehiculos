name: Generar Word

on:
  repository_dispatch:
    types: [crear-docx]

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install python-docx
      - run: python generar_docx.py
      - uses: actions/upload-artifact@v4
        with:
          name: reporte
          path: reporte.docx
